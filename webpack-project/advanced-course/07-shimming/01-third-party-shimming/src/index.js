// import _ from 'lodash';

// console.log(_.join(['hello', 'webpack'], ' '));
// console.log(join(['hello', '斑马'], ' '));
// this.alert('hell momo');

const { file, parse } = require('./globals');
console.log(file);
parse();
